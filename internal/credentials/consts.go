package credentials

const (
	_package = "credentials"

	_fields = `id, photo, gender, title, first_name, middle_names, last_name, birthday, country_of_birth_id,
	country_of_nationality_id, status`
	_entityName       = "Credential"
	_entityNamePlural = "credentials"
	_table            = "core.kyc_credentials"
)
